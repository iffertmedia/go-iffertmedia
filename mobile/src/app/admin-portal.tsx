// Standalone admin entry reachable before a creator has registered on this
// phone. Renders the same passcode-gated Host Console used by the Admin tab,
// so the host can approve creators without joining the leaderboard first.
export { default } from './(tabs)/admin';
